# Docker Compose 環境變數配置 (開發環境)
# 使用方式：docker-compose -f docker-compose.dev.yml up -d
# 複製此檔案為 .env 並根據需要修改值

# Backend 配置 (使用 SQLite，無需 PostgreSQL)
BACKEND_PORT=3000
NODE_ENV=development

# CORS 配置 (允許開發環境的 Vite 開發服務器)
CORS_ORIGIN=http://localhost:5173

# Frontend 配置
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:3000/api
