# Frontend Dockerfile for Development
FROM node:22-alpine

WORKDIR /app

# 安裝依賴
COPY package*.json ./
RUN npm install

# 複製源代碼
COPY . .

# 暴露 Vite 開發服務器端口
EXPOSE 5173

# 開發模式啟動 (--host 允許容器外訪問)
CMD ["npm", "run", "dev", "--", "--host"]
