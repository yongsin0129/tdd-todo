  {
    "install_command": "npm ci --ignore-scripts",
    "build_command": "cp prisma/schema.production.prisma prisma/schema.prisma && npx prisma generate && npm run build",
    "start_command": "npm start"
  }