{
  "install_command": "npm ci --ignore-scripts",
  "build_command": "cp prisma/schema.production.prisma prisma/schema.prisma && npx prisma generate && npm run build && bash scripts/build.sh",
  "start_command": "npx prisma db push && npm start"
}